local tbWnd = Ui:CreateWindow("UILegionMail")

function tbWnd:NotifyEvents()
  return {
    {
      EventNotify.emNOTIFY_SYNC_LEGION_MAIL_COUNT,
      self.Update,
      self
    }
  }
end

tbWnd.tbControls = {
  btnClose = "node/imgBG/btnClose",
  inputMail = "node/imgBG/Content/LegionMail/inputMail",
  btnSend = "node/imgBG/Content/btnSend",
  txtTimesLeftCount = "node/imgBG/Content/LegionMail/txtTimesLeftCount"
}
tbWnd.tbOnClick = {
  btnClose = function(self)
    self:Close()
  end,
  btnSend = function(self)
    self:Send()
  end
}

function tbWnd:OnOpen()
  self.pPanel:Input_SetText(self.inputMail, "")
  self:Update()
  Legion:UpdateMailInfo()
end

function tbWnd:Update()
  local nMailLeftCount = Legion:GetLeftMailCount() or 0
  self.pPanel:Label_SetText(self.txtTimesLeftCount, string.format("%d/%d", nMailLeftCount, Legion.Cfg.nMaxMailCount))
end

function tbWnd:Send()
  local szContent = self.pPanel:Input_GetText(self.inputMail)
  if Legion:SendMail(szContent) then
    self:Close()
  end
end
