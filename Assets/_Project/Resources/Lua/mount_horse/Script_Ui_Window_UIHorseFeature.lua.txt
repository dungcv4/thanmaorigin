local tbWnd = Ui:CreateClass("UIHorseFeature", "UIPanel")
local tbControls = {
  Template = "imgBg/HorseList/Content/Template"
}
tbWnd.tbControls = tbControls
tbWnd.tbOnClick = {}

function tbWnd:OnCreate()
end

function tbWnd:UpdateView()
  self.tbFeature = Horse:GetHorseFeatureList()
  local pHorseItem = me.GetEquipByPos(Item.EQUIPPOS_HORSE)
  if not pHorseItem then
    return
  end
end
